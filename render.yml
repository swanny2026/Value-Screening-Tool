services:
  - type: web
    name: value-screening-tool
    runtime: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: gunicorn --timeout 300 --workers 1 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
